# Aggregate Functions

Aggregate functions calculate a result from multiple rows.

Common functions:
- `COUNT()`
- `SUM()`
- `AVG()`
- `MIN()`
- `MAX()`

## Example
```sql
SELECT AVG(age) AS average_age
FROM student;
```
