# Operators

## Comparison operators
`=`, `<>`, `!=`, `>`, `<`, `>=`, `<=`

## Logical operators
- `AND`
- `OR`
- `NOT`

## Other useful operators
- `BETWEEN`
- `IN`
- `LIKE`
- `IS NULL`

## Example
```sql
SELECT *
FROM student
WHERE age BETWEEN 18 AND 25;
```
