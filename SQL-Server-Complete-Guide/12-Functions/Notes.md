# Functions

SQL Server provides built-in functions for working with data.

Examples:
- `LEN()`
- `UPPER()`
- `LOWER()`
- `GETDATE()`
- `ISNULL()`
- `COALESCE()`

## Example
```sql
SELECT UPPER(name) AS upper_name
FROM student;
```
